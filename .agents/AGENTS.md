# Plain Text Formatting Rule

**NO LATEX MATH FORMATTING:**
- Never use LaTeX syntax or math delimiters (like `$`, `$$`, `\text{}`, `\times`, `\Omega`).
- Always present numbers, equations, component values, and units using plain standard text (e.g., `2x 10k ohm`, `500mA`, `3.0V - 4.2V`, `1.0A`).
